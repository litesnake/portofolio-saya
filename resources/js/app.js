import './bootstrap';
import '../css/app.css';
import AOS from 'aos';
import 'aos/dist/aos.css';

AOS.init();
// Saat ini hanya untuk memastikan DOM loaded
document.addEventListener('DOMContentLoaded', () => {
    console.log("Portofolio siap!");

    // Jika nanti ingin menambahkan Mobile Menu (Hamburger),
    // kodenya bisa ditaruh di sini.
});
